<div id="heading" class="jumbotron">
<h1>Cierres pendientes para presentismo</h1>
<p>Este listado resume el estado de los cierres mensuales para presentismo de cada dependencia a la cual tiene acceso.</p>
<p>Las cruces rojas indican que no se ha enviado el cierre para la dependencia y mes asociado.</p>
</div>

<div id="content">

<div id="pendientes">

<% if (typeof mensaje !== 'undefined') { -%>
<div class="alert alert-danger">
	<strong>ERROR:</strong> <%= mensaje %>
</div>
<% } -%>

<table class="table">
		<tr>
			<th>Dependencia</th>
			<% for(m=3;m<12;m++) { -%>
				<th class="center" width="70px"><%= infoMeses.meses[m].nombre %></th>
			<% } %>
		</tr>
    <% depends.forEach (function(info) { -%>
    <tr>
	    <td><%= info.dependdesc %></td>
			<% for(m=3;m<12;m++) { -%>
			<td class="center"><% if (typeof infoMeses.meses[m].depend[info.dependid] !== 'undefined') { %>
						<span class="glyphicon glyphicon-ok text-success"></span>
					<% } else { %>
						<span class="glyphicon glyphicon-remove text-danger"></span>
					<% } %>
			</td>
			<% } %>

	<% }) -%>
</table>

</div>

</div>
